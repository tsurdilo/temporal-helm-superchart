apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-dashboard-history-health
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  history-health-dashboard.json: |
{{ .Files.Get "files/dashboards/history-health-dashboard.json" | indent 4 }}
