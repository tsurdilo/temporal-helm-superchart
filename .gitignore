# Planning docs — internal only
PLANNING.md

# Local test utilities — not part of the chart
test-worker/
test-backlog.sh

# IDE
.idea/

# Helm dependency cache — regenerated by `helm dependency update`
charts/
Chart.lock
